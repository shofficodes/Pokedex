there are some bug in the design samples, here is how to handle these: 

1. There is a solid and a split color design. I want to make the landing page solid and the overlays with split color, so we have a mix. 
2. Overlay <768px: the close btn is overlaying the content. Place them in the upper right cornor over the fav-icon. 

